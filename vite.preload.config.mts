import { defineConfig } from 'vite';

// The @electron-forge/plugin-vite preload base config supplies the important
// bits (CJS output, `electron` externalized, single-file bundle). Nothing
// project-specific to add here.
export default defineConfig({});
